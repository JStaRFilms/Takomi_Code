<UserControl
    x:Class="TakomiCode.UI.Views.ProjectSelectorView"
    x:Name="ProjectSelectorRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TakomiCode.UI.Views"
    xmlns:viewmodels="using:TakomiCode.UI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#0D0D0D">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar -->
        <Border Grid.Column="0" Background="#111111" BorderBrush="#222222" BorderThickness="0,0,1,0">
            <Grid Padding="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Brand -->
                <StackPanel Grid.Row="0" Spacing="4">
                    <TextBlock Text="V5" Foreground="{StaticResource AccentBrush}" FontWeight="Black" FontSize="18" />
                    <TextBlock Text="GRAPHITE STUDIO" Foreground="White" FontWeight="SemiBold" FontSize="14" CharacterSpacing="100" />
                    <TextBlock Text="TAKOMI CORE ENGINE" Foreground="#666666" FontSize="10" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Sidebar Nav -->
                <StackPanel Grid.Row="1" Margin="0,48,0,0" Spacing="4">
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="#1A1A1A" BorderThickness="0" Padding="12,10">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE8B7;" FontSize="16" Foreground="{StaticResource AccentBrush}" />
                            <TextBlock Text="Projects" Foreground="White" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Padding="12,10">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE7EF;" FontSize="16" Foreground="#666666" />
                            <TextBlock Text="Org Admin" Foreground="#888888" />
                        </StackPanel>
                    </Button>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Padding="12,10">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE713;" FontSize="16" Foreground="#666666" />
                            <TextBlock Text="Global Settings" Foreground="#888888" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Footer Info -->
                <StackPanel Grid.Row="2" Spacing="4">
                    <TextBlock Text="Runtime: v2.4.0-prod" Foreground="#444444" FontSize="10" />
                    <TextBlock Text="Status: Connected" Foreground="#228844" FontSize="10" FontWeight="Bold" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1" Padding="48">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="8">
                <TextBlock Text="Workspace selection" FontSize="28" FontWeight="Light" Foreground="White" />
                <TextBlock Text="Select a recent environment or initialize a new context tree." Foreground="#888888" FontSize="14" />
            </StackPanel>

            <!-- Recent Projects -->
            <Grid Grid.Row="1" Margin="0,32,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="RECENT PROJECTS" Foreground="#666666" FontSize="11" FontWeight="Bold" CharacterSpacing="50" Margin="0,0,0,16" />

                <ListView Grid.Row="1" 
                          ItemsSource="{Binding RecentWorkspaces}"
                          SelectionMode="None"
                          Padding="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0,0,0,12" />
                            <Setter Property="Background" Value="Transparent" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:WorkspaceViewModel">
                            <Button HorizontalAlignment="Stretch" 
                                    HorizontalContentAlignment="Left"
                                    Padding="16"
                                    Background="#141414" 
                                    BorderBrush="#222222"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Command="{Binding ElementName=ProjectSelectorRoot, Path=DataContext.InitializeProjectCommand}"
                                    CommandParameter="{Binding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="40" Height="40" Background="#1A1A1A" CornerRadius="4" Margin="0,0,16,0">
                                        <FontIcon Glyph="&#xE8B7;" Foreground="{StaticResource AccentBrush}" FontSize="20" />
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                        <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" FontSize="14" />
                                        <TextBlock Text="{Binding Path}" Foreground="#666666" FontSize="11" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding LastOpenedFormatted}" Foreground="#444444" FontSize="11" TextAlignment="Right" />
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <!-- Action Bar -->
            <Grid Grid.Row="2" Margin="0,32,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" HorizontalAlignment="Stretch" Height="100" Margin="0,0,12,0" Background="#1A1A1A" BorderBrush="#33ffffff" BorderThickness="1">
                    <StackPanel Spacing="12">
                        <FontIcon Glyph="&#xE710;" Foreground="{StaticResource AccentBrush}" FontSize="24" />
                        <StackPanel Spacing="2">
                            <TextBlock Text="Initialize New" HorizontalAlignment="Center" Foreground="White" FontWeight="SemiBold" />
                            <TextBlock Text="Create a clean workspace" HorizontalAlignment="Center" Foreground="#666666" FontSize="10" />
                        </StackPanel>
                    </StackPanel>
                </Button>

                <Button Grid.Column="1" HorizontalAlignment="Stretch" Height="100" Margin="12,0" Background="#1A1A1A" BorderBrush="#33ffffff" BorderThickness="1">
                    <StackPanel Spacing="12">
                        <FontIcon Glyph="&#xE8B9;" Foreground="{StaticResource AccentBrush}" FontSize="24" />
                        <StackPanel Spacing="2">
                            <TextBlock Text="Clone Repository" HorizontalAlignment="Center" Foreground="White" FontWeight="SemiBold" />
                            <TextBlock Text="Fetch from remote git source" HorizontalAlignment="Center" Foreground="#666666" FontSize="10" />
                        </StackPanel>
                    </StackPanel>
                </Button>

                <Button Grid.Column="2" HorizontalAlignment="Stretch" Height="100" Margin="12,0,0,0" Background="#1A1A1A" BorderBrush="#33ffffff" BorderThickness="1">
                    <StackPanel Spacing="12">
                        <FontIcon Glyph="&#xEE40;" Foreground="{StaticResource AccentBrush}" FontSize="24" />
                        <StackPanel Spacing="2">
                            <TextBlock Text="Join Organization" HorizontalAlignment="Center" Foreground="White" FontWeight="SemiBold" />
                            <TextBlock Text="Collaborate on team nodes" HorizontalAlignment="Center" Foreground="#666666" FontSize="10" />
                        </StackPanel>
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
